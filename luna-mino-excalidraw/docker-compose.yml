version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: luna-mino-excalidraw_server_1
      APP_PORT: 80
      PROXY_AUTH_ADD: "false"

  server:
    image: excalidraw/excalidraw:latest
    restart: unless-stopped